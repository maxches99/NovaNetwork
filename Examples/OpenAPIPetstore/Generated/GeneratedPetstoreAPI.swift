// Generated by nova-openapi from petstore.yaml. Do not edit: regenerating overwrites this file.
// Source document: Petstore 1.4.0
//
// Regenerate with:
//   swift package --allow-writing-to-package-directory nova-openapi \
//     --spec petstore.yaml --output <this file>

import Foundation
import NovaNetworkCore

// MARK: - PetstoreAPI

/// Namespace for values shared by every generated endpoint.
public enum PetstoreAPI {
    /// The server declared by the source document.
    public static let baseURL = URL(string: "https://api.petstore.example.com/v1")!

    /// A decoder configured for this document's payloads.
    ///
    /// Pass it to `execute(endpoint:decoder:)`: the document uses `date-time` values, which the default `JSONDecoder` does not read.
    public static func makeDecoder() -> JSONDecoder {
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .iso8601
        return decoder
    }
}

// MARK: - Models

public struct Pet: Codable, Hashable, Sendable {
    /// The pet's name.
    public var name: String

    public var tag: String?

    public var `default`: String?

    public var dimensions: PetDimensions?

    public var id: Int64

    public var createdAt: Date?

    public var status: PetStatus?

    public var parent: Indirect<Pet>?

    /// Creates a Pet value.
    public init(name: String, tag: String? = nil, `default`: String? = nil, dimensions: PetDimensions? = nil, id: Int64, createdAt: Date? = nil, status: PetStatus? = nil, parent: Indirect<Pet>? = nil) {
        self.name = name
        self.tag = tag
        self.`default` = `default`
        self.dimensions = dimensions
        self.id = id
        self.createdAt = createdAt
        self.status = status
        self.parent = parent
    }

    private enum CodingKeys: String, CodingKey {
        case name
        case tag
        case `default`
        case dimensions
        case id
        case createdAt = "created_at"
        case status
        case parent
    }
}

public struct PetDimensions: Codable, Hashable, Sendable {
    public var width: Double?

    public var height: Double?

    /// Creates a PetDimensions value.
    public init(width: Double? = nil, height: Double? = nil) {
        self.width = width
        self.height = height
    }
}

public struct NewPet: Codable, Hashable, Sendable {
    /// The pet's name.
    public var name: String

    public var tag: String?

    public var `default`: String?

    public var dimensions: NewPetDimensions?

    /// Creates a NewPet value.
    public init(name: String, tag: String? = nil, `default`: String? = nil, dimensions: NewPetDimensions? = nil) {
        self.name = name
        self.tag = tag
        self.`default` = `default`
        self.dimensions = dimensions
    }
}

public struct NewPetDimensions: Codable, Hashable, Sendable {
    public var width: Double?

    public var height: Double?

    /// Creates a NewPetDimensions value.
    public init(width: Double? = nil, height: Double? = nil) {
        self.width = width
        self.height = height
    }
}

/// Availability of a pet.
public enum PetStatus: String, Codable, Hashable, Sendable, CaseIterable, EndpointParameterConvertible {
    case available
    case pending
    case sold
}

public struct PhotoMetadata: Codable, Hashable, Sendable {
    public var labels: PhotoMetadataLabels?

    public var payload: GeneratedAnyJSON?

    public var nickname: String?

    /// Creates a PhotoMetadata value.
    public init(labels: PhotoMetadataLabels? = nil, payload: GeneratedAnyJSON? = nil, nickname: String? = nil) {
        self.labels = labels
        self.payload = payload
        self.nickname = nickname
    }
}

public typealias PhotoMetadataLabels = [String: String]

/// A JSON value from a schema with no single Swift shape, such as a multi-branch `oneOf`.
public enum GeneratedAnyJSON: Codable, Hashable, Sendable {
    case null
    case bool(Bool)
    case number(Double)
    case string(String)
    case array([GeneratedAnyJSON])
    case object([String: GeneratedAnyJSON])

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if container.decodeNil() {
            self = .null
        } else if let value = try? container.decode(Bool.self) {
            self = .bool(value)
        } else if let value = try? container.decode(Double.self) {
            self = .number(value)
        } else if let value = try? container.decode(String.self) {
            self = .string(value)
        } else if let value = try? container.decode([GeneratedAnyJSON].self) {
            self = .array(value)
        } else {
            self = .object(try container.decode([String: GeneratedAnyJSON].self))
        }
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .null: try container.encodeNil()
        case let .bool(value): try container.encode(value)
        case let .number(value): try container.encode(value)
        case let .string(value): try container.encode(value)
        case let .array(value): try container.encode(value)
        case let .object(value): try container.encode(value)
        }
    }
}

/// A value held behind a reference, used where a model would otherwise contain itself.
///
/// Swift rejects a value type that stores itself, and an `Optional` does not help — it still
/// stores its wrapped value inline. Read the value through ``wrapped``.
public struct Indirect<Wrapped: Sendable>: Sendable {
    private final class Storage: Sendable {
        let value: Wrapped

        init(_ value: Wrapped) {
            self.value = value
        }
    }

    private let storage: Storage

    /// The boxed value.
    public var wrapped: Wrapped {
        storage.value
    }

    /// Boxes a value.
    public init(_ wrapped: Wrapped) {
        storage = Storage(wrapped)
    }
}

extension Indirect: Equatable where Wrapped: Equatable {
    public static func == (lhs: Indirect<Wrapped>, rhs: Indirect<Wrapped>) -> Bool {
        lhs.wrapped == rhs.wrapped
    }
}

extension Indirect: Hashable where Wrapped: Hashable {
    public func hash(into hasher: inout Hasher) {
        hasher.combine(wrapped)
    }
}

extension Indirect: Decodable where Wrapped: Decodable {
    public init(from decoder: Decoder) throws {
        self.init(try decoder.singleValueContainer().decode(Wrapped.self))
    }
}

extension Indirect: Encodable where Wrapped: Encodable {
    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        try container.encode(wrapped)
    }
}

// MARK: - Operations

/// Lists pets.
///
/// Returns every pet, newest first.
public struct ListPets: EndpointDefinition {
    public typealias Response = [Pet]

    public var baseURL: URL { PetstoreAPI.baseURL }

    /// How many pets to return.
    public var limit: Int?

    public var tags: [String]?

    public var xRequestId: UUID?

    /// Creates the endpoint.
    public init(limit: Int? = nil, tags: [String]? = nil, xRequestId: UUID? = nil) {
        self.limit = limit
        self.tags = tags
        self.xRequestId = xRequestId
    }

    /// Builds the request for this operation.
    public func makeRequest() throws -> APIRequest {
        var builder = EndpointRequestBuilder(
            method: .get,
            baseURL: baseURL,
            path: "/pets"
        )
        builder.addQuery("limit", limit)
        builder.addQuery("tags", tags, style: .commaSeparated)
        builder.setHeader("X-Request-Id", xRequestId)
        return try builder.build(timeout: timeout, additionalHeaders: additionalHeaders)
    }
}

/// Adds a pet.
public struct CreatePet: EndpointDefinition {
    public typealias Response = Pet

    public var baseURL: URL { PetstoreAPI.baseURL }

    /// The request body.
    public var body: NewPet

    /// Creates the endpoint.
    public init(body: NewPet) {
        self.body = body
    }

    /// Builds the request for this operation.
    public func makeRequest() throws -> APIRequest {
        var builder = EndpointRequestBuilder(
            method: .post,
            baseURL: baseURL,
            path: "/pets"
        )
        try builder.setJSONBody(body, encoder: jsonEncoder)
        return try builder.build(timeout: timeout, additionalHeaders: additionalHeaders)
    }
}

/// Fetches one pet.
public struct GetPetById: EndpointDefinition {
    public typealias Response = Pet

    public var baseURL: URL { PetstoreAPI.baseURL }

    /// Identifier of the pet.
    public var petId: Int64

    /// Creates the endpoint.
    public init(petId: Int64) {
        self.petId = petId
    }

    /// Builds the request for this operation.
    public func makeRequest() throws -> APIRequest {
        var builder = EndpointRequestBuilder(
            method: .get,
            baseURL: baseURL,
            path: "/pets/{petId}"
        )
        try builder.setPath("petId", petId)
        return try builder.build(timeout: timeout, additionalHeaders: additionalHeaders)
    }
}

/// Removes a pet.
public struct DeletePetsByPetId: EndpointDefinition {
    public typealias Response = NoContent

    public var baseURL: URL { PetstoreAPI.baseURL }

    /// Identifier of the pet.
    public var petId: Int64

    /// Creates the endpoint.
    public init(petId: Int64) {
        self.petId = petId
    }

    /// Builds the request for this operation.
    public func makeRequest() throws -> APIRequest {
        var builder = EndpointRequestBuilder(
            method: .delete,
            baseURL: baseURL,
            path: "/pets/{petId}"
        )
        try builder.setPath("petId", petId)
        return try builder.build(timeout: timeout, additionalHeaders: additionalHeaders)
    }
}

@available(*, deprecated)
public struct UpdatePet: EndpointDefinition {
    public typealias Response = Pet

    public var baseURL: URL { PetstoreAPI.baseURL }

    /// Identifier of the pet.
    public var petId: Int64

    /// The request body.
    public var body: NewPet?

    /// Creates the endpoint.
    public init(petId: Int64, body: NewPet? = nil) {
        self.petId = petId
        self.body = body
    }

    /// Builds the request for this operation.
    public func makeRequest() throws -> APIRequest {
        var builder = EndpointRequestBuilder(
            method: .patch,
            baseURL: baseURL,
            path: "/pets/{petId}"
        )
        try builder.setPath("petId", petId)
        if let body {
            try builder.setJSONBody(body, encoder: jsonEncoder)
        }
        return try builder.build(timeout: timeout, additionalHeaders: additionalHeaders)
    }
}

/// Reads photo metadata.
public struct GetPetPhotoMetadata: EndpointDefinition {
    public typealias Response = PhotoMetadata

    public var baseURL: URL { PetstoreAPI.baseURL }

    public var petId: Int64

    /// Creates the endpoint.
    public init(petId: Int64) {
        self.petId = petId
    }

    /// Builds the request for this operation.
    public func makeRequest() throws -> APIRequest {
        var builder = EndpointRequestBuilder(
            method: .get,
            baseURL: baseURL,
            path: "/pets/{petId}/photo"
        )
        try builder.setPath("petId", petId)
        return try builder.build(timeout: timeout, additionalHeaders: additionalHeaders)
    }
}
